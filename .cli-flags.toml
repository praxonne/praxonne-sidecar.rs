# flags-2-env surface for `praxonne-sidecar`
[identity]
service = "praxonne-sidecar"
type_name = "SidecarEnv"

[help]
url = "https://github.com/praxonne/praxonne-sidecar.rs"
columns = ["options", "env", "type", "default", "description"]

[parse]
allow_unknown = false

[flags.bind]
env = "PRAXONNE_SIDECAR_BIND"
long = "bind"
type = "string"
default = "127.0.0.1:9090"
help = "HTTP listen address for /healthz /readyz /metrics."
